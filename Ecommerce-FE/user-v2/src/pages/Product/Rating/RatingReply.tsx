import { RatingReply as RatingReplyType } from 'src/types/rating.type'

type RatingReplyProps = {
    data: RatingReplyType
}

const RatingReply = ({}: RatingReplyProps) => {
    return <div>RatingReply</div>
}

export default RatingReply
